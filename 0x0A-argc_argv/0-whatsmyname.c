#include "main.h"
#include <stdio.h>

/**
 * Main header Prints the name of the program
 * argc: shows Number of command line arguments
 * argv: shows Array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
